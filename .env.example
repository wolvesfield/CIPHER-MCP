# =============================================================================
# CIPHER-MCP  –  Enterprise Secrets Template
# Copy to .env and fill in real values.  NEVER commit .env to source control.
# =============================================================================

# --- GITHUB ENTERPRISE CLOUD (GHEC) -----------------------------------------
# Fine-Grained PAT authorised for the Enterprise SSO tenant.
# Required scopes: repo, read:org, read:enterprise
GITHUB_ENTERPRISE_TOKEN=github_pat_REPLACE_ME

# For GHEC Cloud the REST base URL stays standard.
# Override only for GHES (self-hosted) deployments.
GITHUB_API_URL=https://api.github.com

# Slug of your enterprise organisation (used by github-enterprise-cloud server)
GITHUB_ORG_NAME=your-enterprise-org-slug

# --- DATABASE & CACHE --------------------------------------------------------
# Supabase Postgres connection string
SUPABASE_DB_URL=postgresql://postgres:REPLACE_ME@db.REPLACE_ME.supabase.co:5432/postgres

# Redis connection URL (used by redis-cache server)
REDIS_URL=redis://default:REPLACE_ME@your-redis-endpoint:6379

# --- AI / SEARCH SERVICES ----------------------------------------------------
TAVILY_API_KEY=tvly-REPLACE_ME
MEM0_API_KEY=m0-REPLACE_ME

# --- CLOUD INFRASTRUCTURE ----------------------------------------------------
# Cloudflare (cloudflare-infrastructure server)
CLOUDFLARE_API_TOKEN=REPLACE_ME

# --- CONTAINER ORCHESTRATION -------------------------------------------------
# Absolute path to kubeconfig (kubernetes-cluster server)
KUBECONFIG=/absolute/path/to/.kube/config

# --- BLOCKCHAIN --------------------------------------------------------------
# Solana RPC endpoint (solana-blockchain server)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# --- PRODUCTIVITY & COMMUNICATION --------------------------------------------
# Slack (slack-workspace server)
SLACK_BOT_TOKEN=xoxb-REPLACE_ME
SLACK_TEAM_ID=T0REPLACE_ME

# --- MAPPING -----------------------------------------------------------------
# Google Maps (google-places server)
GOOGLE_MAPS_API_KEY=REPLACE_ME

# --- IDENTITY & ACCESS -------------------------------------------------------
# SailPoint IdentityNow (identitynow-sailpoint server)
SAILPOINT_CLIENT_ID=REPLACE_ME
SAILPOINT_CLIENT_SECRET=REPLACE_ME

# --- FITNESS & WEARABLES -----------------------------------------------------
# Garmin Connect (garmin-connect server)
GARMIN_USERNAME=your@email.com
GARMIN_PASSWORD=REPLACE_ME

# --- DATABASE HUB ------------------------------------------------------------
DBHUB_API_KEY=REPLACE_ME
