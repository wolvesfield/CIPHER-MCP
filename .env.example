# =============================================================================
# CIPHER-MCP  –  Enterprise Secrets Template
# Copy to .env and fill in real values.  NEVER commit .env to source control.
# =============================================================================

# --- GITHUB ENTERPRISE CLOUD (GHEC) -----------------------------------------
# Fine-Grained PAT authorised for the Enterprise SSO tenant.
# Required scopes: repo, read:org, read:enterprise
# Enterprise dashboard : https://github.com/enterprises/nochef
# Organisation URL     : https://github.com/organizations/wolvesfield
GITHUB_ENTERPRISE_TOKEN=github_pat_REPLACE_ME

# For GHEC Cloud the REST base URL stays standard.
# Override only for GHES (self-hosted) deployments.
GITHUB_API_URL=https://api.github.com

# Slug of your enterprise organisation (used by github-enterprise-cloud server)
GITHUB_ORG_NAME=wolvesfield

# --- DATABASE ----------------------------------------------------------------
# Supabase Management API personal access token (sbp_… prefix)
# Generate at: https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=sbp_REPLACE_ME

# --- AI / SEARCH SERVICES ----------------------------------------------------
TAVILY_API_KEY=tvly-REPLACE_ME
MEM0_API_KEY=m0-REPLACE_ME

# --- CONTAINER ORCHESTRATION -------------------------------------------------
# Absolute path to kubeconfig (kubernetes-cluster server)
KUBECONFIG=/absolute/path/to/.kube/config

# --- BLOCKCHAIN --------------------------------------------------------------
# Solana RPC endpoint (solana-blockchain server)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# --- DATABASE HUB ------------------------------------------------------------
# DBHub.io uses its web interface — no API key required.
# Visit: https://dbhub.ai/

# --- COMFY UI GENERATOR ------------------------------------------------------
# Port for the browser-based ComfyUI Generator (comfy_ui_generator.py)
# Default: 7860  — matches the standard ComfyUI port
COMFY_UI_PORT=7860

# WAN generation backend endpoint (optional — only needed for "wan generate")
# Point this at your ComfyUI / RunPod WAN endpoint if you want live generation.
WAN_GENERATOR_URL=REPLACE_ME
WAN_GENERATOR_AUTH_BEARER=REPLACE_ME

# RunPod API key (for pod management via ai_command_center.py)
RUNPOD_API_KEY=REPLACE_ME

# S3-compatible storage (optional — used by ai_command_center.py s3 commands)
S3_ACCESS_KEY_ID=REPLACE_ME
S3_SECRET_ACCESS_KEY=REPLACE_ME
S3_ENDPOINT_URL=REPLACE_ME
S3_REGION=auto
