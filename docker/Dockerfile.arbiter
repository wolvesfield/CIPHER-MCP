FROM python:3.12-slim

WORKDIR /app

COPY . /app

RUN pip install --no-cache-dir \
    fastapi \
    "uvicorn[standard]" \
    httpx \
    pydantic \
    pydantic-ai

CMD ["uvicorn", "arbiter.consensus_arbiter:app", "--host", "0.0.0.0", "--port", "8000"]
