FROM python:3.12-slim

WORKDIR /app

COPY . /app

RUN pip install --no-cache-dir \
    fastapi \
    fastmcp \
    "uvicorn[standard]" \
    httpx \
    pydantic

CMD ["uvicorn", "bridge.mcp_a2a_bridge:app", "--host", "0.0.0.0", "--port", "8001"]
